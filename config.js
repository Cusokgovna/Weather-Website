// Local configuration. This file is intended to be created locally and
// must NOT be committed to source control. Add your OpenWeather API key
// here for local development by copying `config.example.js` -> `config.js`.
// The app will show a helpful error if the key is missing.

window.CONFIG = {
  // Replace the value below with your OpenWeather API key for local dev,
  // or leave the placeholder when using CI/deploy-time injection.
  OPENWEATHER_KEY: "2789ec04fa0f6556d903359dae75d1c7"
};
